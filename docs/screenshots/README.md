# Screenshots

This folder is reserved for exported dashboard or event screenshots.

Included asset:

- `sample-event.jpg`: snapshot captured from the sample-video fallback during a live Pi smoke test

When the detector runs in sample-video mode, the generated event snapshots under `data/media/snapshots/` can be copied here as additional stable demo assets later.
